<template><div class="demo-widget tts-widget" :style="widgetStyle(widget.settings)"><div v-if="widget.settings.showAvatar" class="avatar">🔊</div><div><span v-if="widget.settings.showStatus" class="widget-kicker">{{ event.payload.status||'SIMULATED · READY' }}</span><b v-if="widget.settings.showSpeaker">{{ event.actor.displayName }}</b><p>{{ event.payload.text }}</p></div></div></template>
<script setup>import { widgetStyle,useWidgetEvents } from './widgetHelpers.js'; const props=defineProps({widget:{type:Object,required:true}}); const event=useWidgetEvents(props.widget,{actor:{displayName:'VoiceTester'},payload:{text:'Visual preview only — speech playback is not connected.',status:'SIMULATED'}})</script>
