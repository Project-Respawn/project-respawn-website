export const CHAT_SETTING_SECTIONS = Object.freeze([
  'sources',
  'content',
  'appearance',
  'behaviour',
  'layout',
  'typography',
])

export const CHAT_SOURCE_IDS = Object.freeze({
  TWITCH: 'twitch',
  YOUTUBE: 'youtube',
  TIKTOK: 'tiktok',
})

export const CHAT_BACKGROUND_TYPES = Object.freeze([
  'none',
  'solid',
  'glass',
])

export const CHAT_MESSAGE_DIRECTIONS = Object.freeze([
  'top-to-bottom',
  'bottom-to-top',
])

export const CHAT_ANIMATIONS = Object.freeze([
  'none',
  'fade',
  'slide',
])

export const CHAT_ALIGNMENTS = Object.freeze([
  'left',
  'center',
  'right',
])

export const CHAT_PREVIEW_DEVICES = Object.freeze([
  'desktop',
  'tablet',
  'mobile',
])