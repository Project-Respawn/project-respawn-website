version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npx ampx generate outputs --app-id d2cux232bpa951 --branch development --out-dir .
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*