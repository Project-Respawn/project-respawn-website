version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - amplify pull --d2cux232bpa951 --development
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*