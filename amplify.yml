version: 1
backend:
  phases:
    build:
      commands:
        npx ampx@latest pipeline-deploy --branch master --app-id d2cux232bpa951
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
