
> project-respawn-website@1.0.0 test:overlays
> node --test ./src/features/creator-tools/overlays/tests/*.test.mjs

TAP version 13
# Subtest: shared presentation resolves identical title, message and media for one event and config
ok 1 - shared presentation resolves identical title, message and media for one event and config
  ---
  duration_ms: 1.525339
  type: 'test'
  ...
# Subtest: defensive frontend normalization preserves safe values and neutralizes malformed responses
ok 2 - defensive frontend normalization preserves safe values and neutralizes malformed responses
  ---
  duration_ms: 0.252983
  type: 'test'
  ...
# Subtest: audio lifecycle maps volume, attempts playback, and cleans up on stop and rejection
ok 3 - audio lifecycle maps volume, attempts playback, and cleans up on stop and rejection
  ---
  duration_ms: 2.077562
  type: 'test'
  ...
# Subtest: public Browser Source explicitly selects non-demo runtime mode
ok 4 - public Browser Source explicitly selects non-demo runtime mode
  ---
  duration_ms: 0.841169
  type: 'test'
  ...
# Subtest: live alert starts neutral while editor preview retains its explicit sample
ok 5 - live alert starts neutral while editor preview retains its explicit sample
  ---
  duration_ms: 0.1649
  type: 'test'
  ...
# Subtest: live chat starts empty and hides editor-only preview labeling
ok 6 - live chat starts empty and hides editor-only preview labeling
  ---
  duration_ms: 0.124966
  type: 'test'
  ...
# Subtest: live subscription, raid, and TTS widgets wait for canonical events
ok 7 - live subscription, raid, and TTS widgets wait for canonical events
  ---
  duration_ms: 0.214421
  type: 'test'
  ...
# Subtest: Browser Source owns a fixed transparent viewport and widgets fill outer frames
ok 8 - Browser Source owns a fixed transparent viewport and widgets fill outer frames
  ---
  duration_ms: 0.122487
  type: 'test'
  ...
# Subtest: existing poll applies newer publication scenes without replacing the source connection or credential
ok 9 - existing poll applies newer publication scenes without replacing the source connection or credential
  ---
  duration_ms: 59.527803
  type: 'test'
  ...
# Subtest: config-only updates and reconnect refresh remain functional without rolling the scene back
ok 10 - config-only updates and reconnect refresh remain functional without rolling the scene back
  ---
  duration_ms: 1.952808
  type: 'test'
  ...
# [Vue warn]: withDirectives can only be used inside render functions.
# [Vue warn]: withDirectives can only be used inside render functions.
# [Vue warn]: withDirectives can only be used inside render functions.
# (node:244) ExperimentalWarning: stripTypeScriptTypes is an experimental feature and might change at any time
# (Use `node --trace-warnings ...` to show where the warning was created)
# [Vue warn]: withDirectives can only be used inside render functions.
# Subtest: toolbar labels follow active publication state, including a fresh active-publication lookup
ok 11 - toolbar labels follow active publication state, including a fresh active-publication lookup
  ---
  duration_ms: 58.841947
  type: 'test'
  ...
# Subtest: first creation and repeated saves keep one publication and the issued URL
ok 12 - first creation and repeated saves keep one publication and the issued URL
  ---
  duration_ms: 2.986755
  type: 'test'
  ...
# Subtest: reopening retrieves the same URL; import, preview and repeated saves preserve publication identity
ok 13 - reopening retrieves the same URL; import, preview and repeated saves preserve publication identity
  ---
  duration_ms: 3.064014
  type: 'test'
  ...
# Subtest: panel distinguishes absent publication from lookup failures and shows existing-source recovery
ok 14 - panel distinguishes absent publication from lookup failures and shows existing-source recovery
  ---
  duration_ms: 27.098215
  type: 'test'
  ...
# Subtest: failed or blocked draft saves never publish or retry
ok 15 - failed or blocked draft saves never publish or retry
  ---
  duration_ms: 2.235526
  type: 'test'
  ...
# Subtest: Save Changes keeps the active publication and subsequent Twitch events use its updated scene
ok 16 - Save Changes keeps the active publication and subsequent Twitch events use its updated scene
  ---
  duration_ms: 97.317204
  type: 'test'
  ...
# Subtest: URL is masked on fresh load, toggles explicitly, and copies the real URL without revealing it
ok 17 - URL is masked on fresh load, toggles explicitly, and copies the real URL without revealing it
  ---
  duration_ms: 42.542373
  type: 'test'
  ...
# Subtest: legacy Chat fields normalize into canonical v2 without losing moderation terms
ok 18 - legacy Chat fields normalize into canonical v2 without losing moderation terms
  ---
  duration_ms: 1.432572
  type: 'test'
  ...
# Subtest: missing Chat configuration uses complete current defaults without persisting a preset id
ok 19 - missing Chat configuration uses complete current defaults without persisting a preset id
  ---
  duration_ms: 0.367118
  type: 'test'
  ...
# Subtest: canonical round trip preserves blocked terms and excludes frontend preset state
ok 20 - canonical round trip preserves blocked terms and excludes frontend preset state
  ---
  duration_ms: 2.002192
  type: 'test'
  ...
# Subtest: explicit Brand resolution never substitutes another accessible Brand
ok 21 - explicit Brand resolution never substitutes another accessible Brand
  ---
  duration_ms: 0.276658
  type: 'test'
  ...
# Subtest: Creator Chat save preserves sibling config and only clears dirty state after success
ok 22 - Creator Chat save preserves sibling config and only clears dirty state after success
  ---
  duration_ms: 6.425704
  type: 'test'
  ...
# Subtest: runtime widget prefers canonical Chat v2 and keeps legacy fallback
not ok 23 - runtime widget prefers canonical Chat v2 and keeps legacy fallback
  ---
  duration_ms: 2.134788
  type: 'test'
  location: '/work/src/features/creator-tools/overlays/tests/chatCanonicalConfig.test.mjs:43:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /runtimeConfig\?\.chat \|\| legacyConfig\(\)/. Input:
    
    '<template>\r\n' +
      '  <div class="chat-widget" :class="[`align-${chat.layout.alignment}`, `animation-${chat.behaviour.messageAnimation}`, { smooth: chat.behaviour.smoothScrolling }]" :style="containerStyle" @mouseenter="paused=true" @mouseleave="paused=false">\r\n' +
      '    <div class="chat-lines" :style="linesStyle">\r\n' +
      '      <article v-for="message in visible" :key="message.id" class="chat-line" :class="{ mention: message.isMention && chat.content.highlightMentions, system: message.isSystem }" :style="messageStyle">\r\n' +
      `        <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='left'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>\r\n` +
      '        <span v-if="chat.content.showPlatformIndicator" class="platform" :class="message.platform">{{ platformIcon(message.platform) }}</span>\r\n' +
      '        <span v-if="chat.content.showBadges && message.badges.length" class="badges" :class="`badges-${chat.layout.avatarBadgePosition}`">{{ message.badges.join(\' \') }}</span>\r\n' +
      '        <strong v-if="chat.content.showUsername" :style="usernameStyle(message)">{{ message.user }}</strong>\r\n' +
      '        <span class="message" :style="messageTextStyle(message)">{{ displayText(message.text) }}</span>\r\n' +
      `        <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='right'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>\r\n` +
      '      </article>\r\n' +
      '    </div>\r\n' +
      `    <footer v-if="runtimeMode !== 'browser-source'">{{ preview?.status?.value || (messages.length ? 'Local chat preview' : 'No messages yet · Use Test Chat') }}</footer>\r\n` +
      '  </div>\r\n' +
      '</template>\r\n' +
      '\r\n' +
      '<script setup>\r\n' +
      "import { computed, inject, onMounted, onBeforeUnmount, ref, watch } from 'vue'\r\n" +
      "import { widgetEventBus } from '../../../overlays/widgetEventBus.js'\r\n" +
      "import { normalizeCreatorChatConfig } from '../../../views/chat/chat.config.js'\r\n" +
      '\r\n' +
      "const props = defineProps({ widget:{type:Object,required:true}, runtimeMode:{type:String,default:'editor-preview'}, runtimeConfig:{type:Object,default:null} })\r\n" +
      'const messages = ref([])\r\n' +
      "const preview = inject('overlayChatPreview', null)\r\n" +
      'const seen = new Set()\r\n' +
      'let unsubscribe\r\n' +
      'const paused = ref(false)\r\n' +
      'const timers = new Set()\r\n' +
      '\r\n' +
      'function legacyConfig() {\r\n' +
      '  const value = props.widget.settings || {}\r\n' +
      "  return { enabled:true, maxMessages:value.maxMessages, platforms:String(value.platforms||'Twitch,YouTube,Kick,Discord').split(','), content:{ showUsername:value.showUsername, showBadges:value.showBadges, showEmotes:value.showEmotes, hideBotMessages:value.hideBotMessages, hideCommandMessages:value.hideCommands, messageDisplayDuration:value.messageDuration }, behaviour:{ messageDirection:value.direction==='down'?'bottom-to-top':'top-to-bottom', messageAnimation:value.animation }, appearance:{container:{backgroundColor:value.background,opacity:value.backgroundOpacity,borderRadius:value.cornerRadius}}, typography:{messageSize:value.fontSize} }\r\n" +
      '}\r\n' +
      'const chat = computed(() => normalizeCreatorChatConfig(props.runtimeConfig?.chat || preview?.config?.value || legacyConfig()))\r\n' +
      'const enabledSources = computed(() => new Set(Object.entries(chat.value.sources).filter(([, source]) => source.enabled).map(([id]) => id)))\r\n' +
      'const visible = computed(() => {\r\n' +
      '  if (chat.value.enabled === false) return []\r\n' +
      '  let list = messages.value.filter((message) => enabledSources.value.has(message.platform))\r\n' +
      '  if (chat.value.content.hideBotMessages) list = list.filter((message) => !message.isBot)\r\n' +
      "  if (chat.value.content.hideCommandMessages) list = list.filter((message) => !message.text.trim().startsWith('!'))\r\n" +
      '  list = list.slice(-chat.value.content.maximumVisibleMessages)\r\n' +
      "  return chat.value.behaviour.messageDirection === 'bottom-to-top' ? [...list].reverse() : list\r\n" +
      '})\r\n' +
      '\r\n' +
      'function receiveMessage(next) {\r\n' +
      '  if (!next || !next.payload?.text) return\r\n' +
      '  if (props.widget.enabled === false || props.widget.hidden || (next.targetWidgetId && next.targetWidgetId !== props.widget.id)) return\r\n' +
      '  if (next.id && seen.has(next.id)) return\r\n' +
      '  const blocked = chat.value.blockedTerms || [], text = String(next.payload.text)\r\n' +
      '  if (blocked.some((term) => text.toLowerCase().includes(String(term).toLowerCase()))) return\r\n' +
      "  const item = { id:next.id, platform:String(next.payload.platform||'twitch').toLowerCase(), user:next.actor?.displayName||'Viewer', badges:Array.isArray(next.payload.badges)?next.payload.badges:[], text, timestamp:next.payload.timestamp||new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}), isBot:next.payload.isBot===true, isMention:next.payload.isMention===true, isSystem:next.payload.isSystem===true, usernameColor:next.payload.usernameColor }\r\n" +
      '  messages.value.push(item)\r\n' +
      '  if (next.id) seen.add(next.id)\r\n' +
      '  if (seen.size > 1000) seen.delete(seen.values().next().value)\r\n' +
      '  messages.value = messages.value.slice(-100)\r\n' +
      '  const timer = setTimeout(() => { if (!paused.value || !chat.value.behaviour.pauseOnHover) messages.value = messages.value.filter((message) => message.id !== item.id); timers.delete(timer) }, chat.value.behaviour.messageLifetime * 1000)\r\n' +
      '  timers.add(timer)\r\n' +
      '}\r\n' +
      "onMounted(() => { unsubscribe = widgetEventBus.subscribe('chat.message', receiveMessage) })\r\n" +
      'watch(() => preview?.context?.value, () => { messages.value = []; seen.clear(); timers.forEach(clearTimeout); timers.clear() })\r\n' +
      'onBeforeUnmount(() => { unsubscribe?.(); timers.forEach(clearTimeout) })\r\n' +
      '\r\n' +
      "const hexAlpha = (hex, alpha) => /^#[0-9a-f]{6}$/i.test(hex||'') ? `${hex}${Math.round(alpha*255).toString(16).padStart(2,'0')}` : hex\r\n" +
      "const containerStyle = computed(() => { const c=chat.value.appearance.container,l=chat.value.layout; return { width:l.width==='compact'?'65%':l.width==='medium'?'82%':'100%', marginLeft:l.alignment==='right'?'auto':l.alignment==='center'?'auto':'0', marginRight:l.alignment==='center'?'auto':'0', padding:`${c.padding}px`, borderRadius:`${c.borderRadius}px`, background:c.backgroundType==='none'?'transparent':hexAlpha(c.backgroundColor,c.opacity), border:c.borderEnabled?`1px solid ${c.borderColor}`:'none', backdropFilter:c.backgroundType==='glass'?`blur(${c.blur}px)`:'none', overflow:'hidden' } })\r\n" +
      "const linesStyle = computed(() => ({ display:'flex',flexDirection:'column',gap:`${chat.value.behaviour.messageSpacing}px`,height:'100%',justifyContent:chat.value.behaviour.autoScroll?'flex-end':'flex-start' }))\r\n" +
      "const messageStyle = computed(() => { const m=chat.value.appearance.message,l=chat.value.layout,b=chat.value.behaviour; const speed={slow:0.8,normal:0.4,fast:0.2}[b.animationSpeed]||0.4; return { display:'flex',alignItems:'center',gap:'6px',padding:`${m.verticalPadding}px ${m.horizontalPadding}px`,borderRadius:`${m.borderRadius}px`,background:m.backgroundType==='none'?'transparent':hexAlpha(m.backgroundColor,m.opacity),borderBottom:l.showMessageSeparators?`1px ${l.separatorStyle} ${l.separatorColor}`:'none',textAlign:l.alignment,animationDuration:`${speed}s`,transitionDuration:`${b.fadeDuration}s` } })\r\n" +
      "const messageTextStyle = (message) => ({ color:message.isSystem?chat.value.typography.systemMessageColor:chat.value.typography.messageColor,fontFamily:chat.value.typography.messageFont,fontWeight:chat.value.typography.messageWeight,fontSize:`${chat.value.typography.messageSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })\r\n" +
      "const usernameStyle = (message) => ({ color:message.usernameColor||chat.value.typography.usernameColor,fontFamily:chat.value.typography.usernameFont,fontWeight:chat.value.typography.usernameWeight,fontSize:`${chat.value.typography.usernameSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })\r\n" +
      'const supportStyle = computed(() => ({ color:chat.value.typography.timestampColor }))\r\n' +
      "const platformIcon = (platform) => ({twitch:'T',youtube:'▶',tiktok:'♪',discord:'D',kick:'K'}[platform]||'•')\r\n" +
      "const displayText = (text) => chat.value.content.showEmotes ? text : String(text).replace(/[\\p{Extended_Pictographic}\\uFE0F]/gu, '')\r\n" +
      '</script>\r\n' +
      '\r\n' +
      '<style scoped>\r\n' +
      '.chat-widget{box-sizing:border-box;height:100%;color:#fff}.chat-line{width:100%;box-sizing:border-box;overflow-wrap:anywhere}.chat-line.mention{outline:1px solid rgba(139,92,246,.5)}.platform{display:inline-grid;width:20px;height:20px;flex:none;place-items:center;border-radius:4px;background:#9146ff;font-size:10px;font-weight:800}.platform.youtube{background:#dc2626}.platform.kick{color:#111;background:#53fc18}.platform.discord{background:#5865f2}.badges-right{order:5}.timestamp{font-size:10px}.message{min-width:0}.animation-fade .chat-line{animation:chat-fade both}.animation-slide .chat-line{animation:chat-slide both}.smooth .chat-lines{scroll-behavior:smooth}@keyframes chat-fade{from{opacity:0}to{opacity:1}}@keyframes chat-slide{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}\r\n' +
      '.chat-widget footer{margin-top:8px;color:#94a3b8;font-size:10px}\r\n' +
      '</style>\r\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    <template>
      <div class="chat-widget" :class="[`align-${chat.layout.alignment}`, `animation-${chat.behaviour.messageAnimation}`, { smooth: chat.behaviour.smoothScrolling }]" :style="containerStyle" @mouseenter="paused=true" @mouseleave="paused=false">
        <div class="chat-lines" :style="linesStyle">
          <article v-for="message in visible" :key="message.id" class="chat-line" :class="{ mention: message.isMention && chat.content.highlightMentions, system: message.isSystem }" :style="messageStyle">
            <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='left'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>
            <span v-if="chat.content.showPlatformIndicator" class="platform" :class="message.platform">{{ platformIcon(message.platform) }}</span>
            <span v-if="chat.content.showBadges && message.badges.length" class="badges" :class="`badges-${chat.layout.avatarBadgePosition}`">{{ message.badges.join(' ') }}</span>
            <strong v-if="chat.content.showUsername" :style="usernameStyle(message)">{{ message.user }}</strong>
            <span class="message" :style="messageTextStyle(message)">{{ displayText(message.text) }}</span>
            <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='right'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>
          </article>
        </div>
        <footer v-if="runtimeMode !== 'browser-source'">{{ preview?.status?.value || (messages.length ? 'Local chat preview' : 'No messages yet · Use Test Chat') }}</footer>
      </div>
    </template>
    
    <script setup>
    import { computed, inject, onMounted, onBeforeUnmount, ref, watch } from 'vue'
    import { widgetEventBus } from '../../../overlays/widgetEventBus.js'
    import { normalizeCreatorChatConfig } from '../../../views/chat/chat.config.js'
    
    const props = defineProps({ widget:{type:Object,required:true}, runtimeMode:{type:String,default:'editor-preview'}, runtimeConfig:{type:Object,default:null} })
    const messages = ref([])
    const preview = inject('overlayChatPreview', null)
    const seen = new Set()
    let unsubscribe
    const paused = ref(false)
    const timers = new Set()
    
    function legacyConfig() {
      const value = props.widget.settings || {}
      return { enabled:true, maxMessages:value.maxMessages, platforms:String(value.platforms||'Twitch,YouTube,Kick,Discord').split(','), content:{ showUsername:value.showUsername, showBadges:value.showBadges, showEmotes:value.showEmotes, hideBotMessages:value.hideBotMessages, hideCommandMessages:value.hideCommands, messageDisplayDuration:value.messageDuration }, behaviour:{ messageDirection:value.direction==='down'?'bottom-to-top':'top-to-bottom', messageAnimation:value.animation }, appearance:{container:{backgroundColor:value.background,opacity:value.backgroundOpacity,borderRadius:value.cornerRadius}}, typography:{messageSize:value.fontSize} }
    }
    const chat = computed(() => normalizeCreatorChatConfig(props.runtimeConfig?.chat || preview?.config?.value || legacyConfig()))
    const enabledSources = computed(() => new Set(Object.entries(chat.value.sources).filter(([, source]) => source.enabled).map(([id]) => id)))
    const visible = computed(() => {
      if (chat.value.enabled === false) return []
      let list = messages.value.filter((message) => enabledSources.value.has(message.platform))
      if (chat.value.content.hideBotMessages) list = list.filter((message) => !message.isBot)
      if (chat.value.content.hideCommandMessages) list = list.filter((message) => !message.text.trim().startsWith('!'))
      list = list.slice(-chat.value.content.maximumVisibleMessages)
      return chat.value.behaviour.messageDirection === 'bottom-to-top' ? [...list].reverse() : list
    })
    
    function receiveMessage(next) {
      if (!next || !next.payload?.text) return
      if (props.widget.enabled === false || props.widget.hidden || (next.targetWidgetId && next.targetWidgetId !== props.widget.id)) return
      if (next.id && seen.has(next.id)) return
      const blocked = chat.value.blockedTerms || [], text = String(next.payload.text)
      if (blocked.some((term) => text.toLowerCase().includes(String(term).toLowerCase()))) return
      const item = { id:next.id, platform:String(next.payload.platform||'twitch').toLowerCase(), user:next.actor?.displayName||'Viewer', badges:Array.isArray(next.payload.badges)?next.payload.badges:[], text, timestamp:next.payload.timestamp||new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}), isBot:next.payload.isBot===true, isMention:next.payload.isMention===true, isSystem:next.payload.isSystem===true, usernameColor:next.payload.usernameColor }
      messages.value.push(item)
      if (next.id) seen.add(next.id)
      if (seen.size > 1000) seen.delete(seen.values().next().value)
      messages.value = messages.value.slice(-100)
      const timer = setTimeout(() => { if (!paused.value || !chat.value.behaviour.pauseOnHover) messages.value = messages.value.filter((message) => message.id !== item.id); timers.delete(timer) }, chat.value.behaviour.messageLifetime * 1000)
      timers.add(timer)
    }
    onMounted(() => { unsubscribe = widgetEventBus.subscribe('chat.message', receiveMessage) })
    watch(() => preview?.context?.value, () => { messages.value = []; seen.clear(); timers.forEach(clearTimeout); timers.clear() })
    onBeforeUnmount(() => { unsubscribe?.(); timers.forEach(clearTimeout) })
    
    const hexAlpha = (hex, alpha) => /^#[0-9a-f]{6}$/i.test(hex||'') ? `${hex}${Math.round(alpha*255).toString(16).padStart(2,'0')}` : hex
    const containerStyle = computed(() => { const c=chat.value.appearance.container,l=chat.value.layout; return { width:l.width==='compact'?'65%':l.width==='medium'?'82%':'100%', marginLeft:l.alignment==='right'?'auto':l.alignment==='center'?'auto':'0', marginRight:l.alignment==='center'?'auto':'0', padding:`${c.padding}px`, borderRadius:`${c.borderRadius}px`, background:c.backgroundType==='none'?'transparent':hexAlpha(c.backgroundColor,c.opacity), border:c.borderEnabled?`1px solid ${c.borderColor}`:'none', backdropFilter:c.backgroundType==='glass'?`blur(${c.blur}px)`:'none', overflow:'hidden' } })
    const linesStyle = computed(() => ({ display:'flex',flexDirection:'column',gap:`${chat.value.behaviour.messageSpacing}px`,height:'100%',justifyContent:chat.value.behaviour.autoScroll?'flex-end':'flex-start' }))
    const messageStyle = computed(() => { const m=chat.value.appearance.message,l=chat.value.layout,b=chat.value.behaviour; const speed={slow:0.8,normal:0.4,fast:0.2}[b.animationSpeed]||0.4; return { display:'flex',alignItems:'center',gap:'6px',padding:`${m.verticalPadding}px ${m.horizontalPadding}px`,borderRadius:`${m.borderRadius}px`,background:m.backgroundType==='none'?'transparent':hexAlpha(m.backgroundColor,m.opacity),borderBottom:l.showMessageSeparators?`1px ${l.separatorStyle} ${l.separatorColor}`:'none',textAlign:l.alignment,animationDuration:`${speed}s`,transitionDuration:`${b.fadeDuration}s` } })
    const messageTextStyle = (message) => ({ color:message.isSystem?chat.value.typography.systemMessageColor:chat.value.typography.messageColor,fontFamily:chat.value.typography.messageFont,fontWeight:chat.value.typography.messageWeight,fontSize:`${chat.value.typography.messageSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })
    const usernameStyle = (message) => ({ color:message.usernameColor||chat.value.typography.usernameColor,fontFamily:chat.value.typography.usernameFont,fontWeight:chat.value.typography.usernameWeight,fontSize:`${chat.value.typography.usernameSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })
    const supportStyle = computed(() => ({ color:chat.value.typography.timestampColor }))
    const platformIcon = (platform) => ({twitch:'T',youtube:'▶',tiktok:'♪',discord:'D',kick:'K'}[platform]||'•')
    const displayText = (text) => chat.value.content.showEmotes ? text : String(text).replace(/[\p{Extended_Pictographic}\uFE0F]/gu, '')
    </script>
    
    <style scoped>
    .chat-widget{box-sizing:border-box;height:100%;color:#fff}.chat-line{width:100%;box-sizing:border-box;overflow-wrap:anywhere}.chat-line.mention{outline:1px solid rgba(139,92,246,.5)}.platform{display:inline-grid;width:20px;height:20px;flex:none;place-items:center;border-radius:4px;background:#9146ff;font-size:10px;font-weight:800}.platform.youtube{background:#dc2626}.platform.kick{color:#111;background:#53fc18}.platform.discord{background:#5865f2}.badges-right{order:5}.timestamp{font-size:10px}.message{min-width:0}.animation-fade .chat-line{animation:chat-fade both}.animation-slide .chat-line{animation:chat-slide both}.smooth .chat-lines{scroll-behavior:smooth}@keyframes chat-fade{from{opacity:0}to{opacity:1}}@keyframes chat-slide{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}
    .chat-widget footer{margin-top:8px;color:#94a3b8;font-size:10px}
    </style>
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///work/src/features/creator-tools/overlays/tests/chatCanonicalConfig.test.mjs:45:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: Creator preview and Browser Source widget share the same normalizer
ok 24 - Creator preview and Browser Source widget share the same normalizer
  ---
  duration_ms: 1.957293
  type: 'test'
  ...
# Subtest: missing and legacy Chat configurations normalize into the versioned frontend contract
ok 25 - missing and legacy Chat configurations normalize into the versioned frontend contract
  ---
  duration_ms: 1.964183
  type: 'test'
  ...
# Subtest: canonical Chat round trip preserves Moderation terms and rejects invalid client values
ok 26 - canonical Chat round trip preserves Moderation terms and rejects invalid client values
  ---
  duration_ms: 3.032335
  type: 'test'
  ...
# Subtest: explicit selected Brand resolution isolates workspace and Brand pairs
ok 27 - explicit selected Brand resolution isolates workspace and Brand pairs
  ---
  duration_ms: 0.187471
  type: 'test'
  ...
# Subtest: Creator Chat save preserves sibling config and only clears dirty after success
ok 28 - Creator Chat save preserves sibling config and only clears dirty after success
  ---
  duration_ms: 6.056069
  type: 'test'
  ...
# Subtest: Browser Source forwards refreshed Chat config and twitch-chat prefers canonical runtime config
not ok 29 - Browser Source forwards refreshed Chat config and twitch-chat prefers canonical runtime config
  ---
  duration_ms: 2.990957
  type: 'test'
  location: '/work/src/features/creator-tools/overlays/tests/creatorChatConfig.test.mjs:53:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /props\.runtimeConfig\?\.chat \|\| legacyConfig\(\)/. Input:
    
    '<template>\r\n' +
      '  <div class="chat-widget" :class="[`align-${chat.layout.alignment}`, `animation-${chat.behaviour.messageAnimation}`, { smooth: chat.behaviour.smoothScrolling }]" :style="containerStyle" @mouseenter="paused=true" @mouseleave="paused=false">\r\n' +
      '    <div class="chat-lines" :style="linesStyle">\r\n' +
      '      <article v-for="message in visible" :key="message.id" class="chat-line" :class="{ mention: message.isMention && chat.content.highlightMentions, system: message.isSystem }" :style="messageStyle">\r\n' +
      `        <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='left'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>\r\n` +
      '        <span v-if="chat.content.showPlatformIndicator" class="platform" :class="message.platform">{{ platformIcon(message.platform) }}</span>\r\n' +
      '        <span v-if="chat.content.showBadges && message.badges.length" class="badges" :class="`badges-${chat.layout.avatarBadgePosition}`">{{ message.badges.join(\' \') }}</span>\r\n' +
      '        <strong v-if="chat.content.showUsername" :style="usernameStyle(message)">{{ message.user }}</strong>\r\n' +
      '        <span class="message" :style="messageTextStyle(message)">{{ displayText(message.text) }}</span>\r\n' +
      `        <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='right'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>\r\n` +
      '      </article>\r\n' +
      '    </div>\r\n' +
      `    <footer v-if="runtimeMode !== 'browser-source'">{{ preview?.status?.value || (messages.length ? 'Local chat preview' : 'No messages yet · Use Test Chat') }}</footer>\r\n` +
      '  </div>\r\n' +
      '</template>\r\n' +
      '\r\n' +
      '<script setup>\r\n' +
      "import { computed, inject, onMounted, onBeforeUnmount, ref, watch } from 'vue'\r\n" +
      "import { widgetEventBus } from '../../../overlays/widgetEventBus.js'\r\n" +
      "import { normalizeCreatorChatConfig } from '../../../views/chat/chat.config.js'\r\n" +
      '\r\n' +
      "const props = defineProps({ widget:{type:Object,required:true}, runtimeMode:{type:String,default:'editor-preview'}, runtimeConfig:{type:Object,default:null} })\r\n" +
      'const messages = ref([])\r\n' +
      "const preview = inject('overlayChatPreview', null)\r\n" +
      'const seen = new Set()\r\n' +
      'let unsubscribe\r\n' +
      'const paused = ref(false)\r\n' +
      'const timers = new Set()\r\n' +
      '\r\n' +
      'function legacyConfig() {\r\n' +
      '  const value = props.widget.settings || {}\r\n' +
      "  return { enabled:true, maxMessages:value.maxMessages, platforms:String(value.platforms||'Twitch,YouTube,Kick,Discord').split(','), content:{ showUsername:value.showUsername, showBadges:value.showBadges, showEmotes:value.showEmotes, hideBotMessages:value.hideBotMessages, hideCommandMessages:value.hideCommands, messageDisplayDuration:value.messageDuration }, behaviour:{ messageDirection:value.direction==='down'?'bottom-to-top':'top-to-bottom', messageAnimation:value.animation }, appearance:{container:{backgroundColor:value.background,opacity:value.backgroundOpacity,borderRadius:value.cornerRadius}}, typography:{messageSize:value.fontSize} }\r\n" +
      '}\r\n' +
      'const chat = computed(() => normalizeCreatorChatConfig(props.runtimeConfig?.chat || preview?.config?.value || legacyConfig()))\r\n' +
      'const enabledSources = computed(() => new Set(Object.entries(chat.value.sources).filter(([, source]) => source.enabled).map(([id]) => id)))\r\n' +
      'const visible = computed(() => {\r\n' +
      '  if (chat.value.enabled === false) return []\r\n' +
      '  let list = messages.value.filter((message) => enabledSources.value.has(message.platform))\r\n' +
      '  if (chat.value.content.hideBotMessages) list = list.filter((message) => !message.isBot)\r\n' +
      "  if (chat.value.content.hideCommandMessages) list = list.filter((message) => !message.text.trim().startsWith('!'))\r\n" +
      '  list = list.slice(-chat.value.content.maximumVisibleMessages)\r\n' +
      "  return chat.value.behaviour.messageDirection === 'bottom-to-top' ? [...list].reverse() : list\r\n" +
      '})\r\n' +
      '\r\n' +
      'function receiveMessage(next) {\r\n' +
      '  if (!next || !next.payload?.text) return\r\n' +
      '  if (props.widget.enabled === false || props.widget.hidden || (next.targetWidgetId && next.targetWidgetId !== props.widget.id)) return\r\n' +
      '  if (next.id && seen.has(next.id)) return\r\n' +
      '  const blocked = chat.value.blockedTerms || [], text = String(next.payload.text)\r\n' +
      '  if (blocked.some((term) => text.toLowerCase().includes(String(term).toLowerCase()))) return\r\n' +
      "  const item = { id:next.id, platform:String(next.payload.platform||'twitch').toLowerCase(), user:next.actor?.displayName||'Viewer', badges:Array.isArray(next.payload.badges)?next.payload.badges:[], text, timestamp:next.payload.timestamp||new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}), isBot:next.payload.isBot===true, isMention:next.payload.isMention===true, isSystem:next.payload.isSystem===true, usernameColor:next.payload.usernameColor }\r\n" +
      '  messages.value.push(item)\r\n' +
      '  if (next.id) seen.add(next.id)\r\n' +
      '  if (seen.size > 1000) seen.delete(seen.values().next().value)\r\n' +
      '  messages.value = messages.value.slice(-100)\r\n' +
      '  const timer = setTimeout(() => { if (!paused.value || !chat.value.behaviour.pauseOnHover) messages.value = messages.value.filter((message) => message.id !== item.id); timers.delete(timer) }, chat.value.behaviour.messageLifetime * 1000)\r\n' +
      '  timers.add(timer)\r\n' +
      '}\r\n' +
      "onMounted(() => { unsubscribe = widgetEventBus.subscribe('chat.message', receiveMessage) })\r\n" +
      'watch(() => preview?.context?.value, () => { messages.value = []; seen.clear(); timers.forEach(clearTimeout); timers.clear() })\r\n' +
      'onBeforeUnmount(() => { unsubscribe?.(); timers.forEach(clearTimeout) })\r\n' +
      '\r\n' +
      "const hexAlpha = (hex, alpha) => /^#[0-9a-f]{6}$/i.test(hex||'') ? `${hex}${Math.round(alpha*255).toString(16).padStart(2,'0')}` : hex\r\n" +
      "const containerStyle = computed(() => { const c=chat.value.appearance.container,l=chat.value.layout; return { width:l.width==='compact'?'65%':l.width==='medium'?'82%':'100%', marginLeft:l.alignment==='right'?'auto':l.alignment==='center'?'auto':'0', marginRight:l.alignment==='center'?'auto':'0', padding:`${c.padding}px`, borderRadius:`${c.borderRadius}px`, background:c.backgroundType==='none'?'transparent':hexAlpha(c.backgroundColor,c.opacity), border:c.borderEnabled?`1px solid ${c.borderColor}`:'none', backdropFilter:c.backgroundType==='glass'?`blur(${c.blur}px)`:'none', overflow:'hidden' } })\r\n" +
      "const linesStyle = computed(() => ({ display:'flex',flexDirection:'column',gap:`${chat.value.behaviour.messageSpacing}px`,height:'100%',justifyContent:chat.value.behaviour.autoScroll?'flex-end':'flex-start' }))\r\n" +
      "const messageStyle = computed(() => { const m=chat.value.appearance.message,l=chat.value.layout,b=chat.value.behaviour; const speed={slow:0.8,normal:0.4,fast:0.2}[b.animationSpeed]||0.4; return { display:'flex',alignItems:'center',gap:'6px',padding:`${m.verticalPadding}px ${m.horizontalPadding}px`,borderRadius:`${m.borderRadius}px`,background:m.backgroundType==='none'?'transparent':hexAlpha(m.backgroundColor,m.opacity),borderBottom:l.showMessageSeparators?`1px ${l.separatorStyle} ${l.separatorColor}`:'none',textAlign:l.alignment,animationDuration:`${speed}s`,transitionDuration:`${b.fadeDuration}s` } })\r\n" +
      "const messageTextStyle = (message) => ({ color:message.isSystem?chat.value.typography.systemMessageColor:chat.value.typography.messageColor,fontFamily:chat.value.typography.messageFont,fontWeight:chat.value.typography.messageWeight,fontSize:`${chat.value.typography.messageSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })\r\n" +
      "const usernameStyle = (message) => ({ color:message.usernameColor||chat.value.typography.usernameColor,fontFamily:chat.value.typography.usernameFont,fontWeight:chat.value.typography.usernameWeight,fontSize:`${chat.value.typography.usernameSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })\r\n" +
      'const supportStyle = computed(() => ({ color:chat.value.typography.timestampColor }))\r\n' +
      "const platformIcon = (platform) => ({twitch:'T',youtube:'▶',tiktok:'♪',discord:'D',kick:'K'}[platform]||'•')\r\n" +
      "const displayText = (text) => chat.value.content.showEmotes ? text : String(text).replace(/[\\p{Extended_Pictographic}\\uFE0F]/gu, '')\r\n" +
      '</script>\r\n' +
      '\r\n' +
      '<style scoped>\r\n' +
      '.chat-widget{box-sizing:border-box;height:100%;color:#fff}.chat-line{width:100%;box-sizing:border-box;overflow-wrap:anywhere}.chat-line.mention{outline:1px solid rgba(139,92,246,.5)}.platform{display:inline-grid;width:20px;height:20px;flex:none;place-items:center;border-radius:4px;background:#9146ff;font-size:10px;font-weight:800}.platform.youtube{background:#dc2626}.platform.kick{color:#111;background:#53fc18}.platform.discord{background:#5865f2}.badges-right{order:5}.timestamp{font-size:10px}.message{min-width:0}.animation-fade .chat-line{animation:chat-fade both}.animation-slide .chat-line{animation:chat-slide both}.smooth .chat-lines{scroll-behavior:smooth}@keyframes chat-fade{from{opacity:0}to{opacity:1}}@keyframes chat-slide{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}\r\n' +
      '.chat-widget footer{margin-top:8px;color:#94a3b8;font-size:10px}\r\n' +
      '</style>\r\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    <template>
      <div class="chat-widget" :class="[`align-${chat.layout.alignment}`, `animation-${chat.behaviour.messageAnimation}`, { smooth: chat.behaviour.smoothScrolling }]" :style="containerStyle" @mouseenter="paused=true" @mouseleave="paused=false">
        <div class="chat-lines" :style="linesStyle">
          <article v-for="message in visible" :key="message.id" class="chat-line" :class="{ mention: message.isMention && chat.content.highlightMentions, system: message.isSystem }" :style="messageStyle">
            <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='left'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>
            <span v-if="chat.content.showPlatformIndicator" class="platform" :class="message.platform">{{ platformIcon(message.platform) }}</span>
            <span v-if="chat.content.showBadges && message.badges.length" class="badges" :class="`badges-${chat.layout.avatarBadgePosition}`">{{ message.badges.join(' ') }}</span>
            <strong v-if="chat.content.showUsername" :style="usernameStyle(message)">{{ message.user }}</strong>
            <span class="message" :style="messageTextStyle(message)">{{ displayText(message.text) }}</span>
            <span v-if="chat.content.showTimestamps && chat.layout.timestampPosition==='right'" class="timestamp" :style="supportStyle">{{ message.timestamp }}</span>
          </article>
        </div>
        <footer v-if="runtimeMode !== 'browser-source'">{{ preview?.status?.value || (messages.length ? 'Local chat preview' : 'No messages yet · Use Test Chat') }}</footer>
      </div>
    </template>
    
    <script setup>
    import { computed, inject, onMounted, onBeforeUnmount, ref, watch } from 'vue'
    import { widgetEventBus } from '../../../overlays/widgetEventBus.js'
    import { normalizeCreatorChatConfig } from '../../../views/chat/chat.config.js'
    
    const props = defineProps({ widget:{type:Object,required:true}, runtimeMode:{type:String,default:'editor-preview'}, runtimeConfig:{type:Object,default:null} })
    const messages = ref([])
    const preview = inject('overlayChatPreview', null)
    const seen = new Set()
    let unsubscribe
    const paused = ref(false)
    const timers = new Set()
    
    function legacyConfig() {
      const value = props.widget.settings || {}
      return { enabled:true, maxMessages:value.maxMessages, platforms:String(value.platforms||'Twitch,YouTube,Kick,Discord').split(','), content:{ showUsername:value.showUsername, showBadges:value.showBadges, showEmotes:value.showEmotes, hideBotMessages:value.hideBotMessages, hideCommandMessages:value.hideCommands, messageDisplayDuration:value.messageDuration }, behaviour:{ messageDirection:value.direction==='down'?'bottom-to-top':'top-to-bottom', messageAnimation:value.animation }, appearance:{container:{backgroundColor:value.background,opacity:value.backgroundOpacity,borderRadius:value.cornerRadius}}, typography:{messageSize:value.fontSize} }
    }
    const chat = computed(() => normalizeCreatorChatConfig(props.runtimeConfig?.chat || preview?.config?.value || legacyConfig()))
    const enabledSources = computed(() => new Set(Object.entries(chat.value.sources).filter(([, source]) => source.enabled).map(([id]) => id)))
    const visible = computed(() => {
      if (chat.value.enabled === false) return []
      let list = messages.value.filter((message) => enabledSources.value.has(message.platform))
      if (chat.value.content.hideBotMessages) list = list.filter((message) => !message.isBot)
      if (chat.value.content.hideCommandMessages) list = list.filter((message) => !message.text.trim().startsWith('!'))
      list = list.slice(-chat.value.content.maximumVisibleMessages)
      return chat.value.behaviour.messageDirection === 'bottom-to-top' ? [...list].reverse() : list
    })
    
    function receiveMessage(next) {
      if (!next || !next.payload?.text) return
      if (props.widget.enabled === false || props.widget.hidden || (next.targetWidgetId && next.targetWidgetId !== props.widget.id)) return
      if (next.id && seen.has(next.id)) return
      const blocked = chat.value.blockedTerms || [], text = String(next.payload.text)
      if (blocked.some((term) => text.toLowerCase().includes(String(term).toLowerCase()))) return
      const item = { id:next.id, platform:String(next.payload.platform||'twitch').toLowerCase(), user:next.actor?.displayName||'Viewer', badges:Array.isArray(next.payload.badges)?next.payload.badges:[], text, timestamp:next.payload.timestamp||new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}), isBot:next.payload.isBot===true, isMention:next.payload.isMention===true, isSystem:next.payload.isSystem===true, usernameColor:next.payload.usernameColor }
      messages.value.push(item)
      if (next.id) seen.add(next.id)
      if (seen.size > 1000) seen.delete(seen.values().next().value)
      messages.value = messages.value.slice(-100)
      const timer = setTimeout(() => { if (!paused.value || !chat.value.behaviour.pauseOnHover) messages.value = messages.value.filter((message) => message.id !== item.id); timers.delete(timer) }, chat.value.behaviour.messageLifetime * 1000)
      timers.add(timer)
    }
    onMounted(() => { unsubscribe = widgetEventBus.subscribe('chat.message', receiveMessage) })
    watch(() => preview?.context?.value, () => { messages.value = []; seen.clear(); timers.forEach(clearTimeout); timers.clear() })
    onBeforeUnmount(() => { unsubscribe?.(); timers.forEach(clearTimeout) })
    
    const hexAlpha = (hex, alpha) => /^#[0-9a-f]{6}$/i.test(hex||'') ? `${hex}${Math.round(alpha*255).toString(16).padStart(2,'0')}` : hex
    const containerStyle = computed(() => { const c=chat.value.appearance.container,l=chat.value.layout; return { width:l.width==='compact'?'65%':l.width==='medium'?'82%':'100%', marginLeft:l.alignment==='right'?'auto':l.alignment==='center'?'auto':'0', marginRight:l.alignment==='center'?'auto':'0', padding:`${c.padding}px`, borderRadius:`${c.borderRadius}px`, background:c.backgroundType==='none'?'transparent':hexAlpha(c.backgroundColor,c.opacity), border:c.borderEnabled?`1px solid ${c.borderColor}`:'none', backdropFilter:c.backgroundType==='glass'?`blur(${c.blur}px)`:'none', overflow:'hidden' } })
    const linesStyle = computed(() => ({ display:'flex',flexDirection:'column',gap:`${chat.value.behaviour.messageSpacing}px`,height:'100%',justifyContent:chat.value.behaviour.autoScroll?'flex-end':'flex-start' }))
    const messageStyle = computed(() => { const m=chat.value.appearance.message,l=chat.value.layout,b=chat.value.behaviour; const speed={slow:0.8,normal:0.4,fast:0.2}[b.animationSpeed]||0.4; return { display:'flex',alignItems:'center',gap:'6px',padding:`${m.verticalPadding}px ${m.horizontalPadding}px`,borderRadius:`${m.borderRadius}px`,background:m.backgroundType==='none'?'transparent':hexAlpha(m.backgroundColor,m.opacity),borderBottom:l.showMessageSeparators?`1px ${l.separatorStyle} ${l.separatorColor}`:'none',textAlign:l.alignment,animationDuration:`${speed}s`,transitionDuration:`${b.fadeDuration}s` } })
    const messageTextStyle = (message) => ({ color:message.isSystem?chat.value.typography.systemMessageColor:chat.value.typography.messageColor,fontFamily:chat.value.typography.messageFont,fontWeight:chat.value.typography.messageWeight,fontSize:`${chat.value.typography.messageSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })
    const usernameStyle = (message) => ({ color:message.usernameColor||chat.value.typography.usernameColor,fontFamily:chat.value.typography.usernameFont,fontWeight:chat.value.typography.usernameWeight,fontSize:`${chat.value.typography.usernameSize}px`,textShadow:chat.value.typography.textShadow?'0 1px 4px rgba(0,0,0,.75)':'none' })
    const supportStyle = computed(() => ({ color:chat.value.typography.timestampColor }))
    const platformIcon = (platform) => ({twitch:'T',youtube:'▶',tiktok:'♪',discord:'D',kick:'K'}[platform]||'•')
    const displayText = (text) => chat.value.content.showEmotes ? text : String(text).replace(/[\p{Extended_Pictographic}\uFE0F]/gu, '')
    </script>
    
    <style scoped>
    .chat-widget{box-sizing:border-box;height:100%;color:#fff}.chat-line{width:100%;box-sizing:border-box;overflow-wrap:anywhere}.chat-line.mention{outline:1px solid rgba(139,92,246,.5)}.platform{display:inline-grid;width:20px;height:20px;flex:none;place-items:center;border-radius:4px;background:#9146ff;font-size:10px;font-weight:800}.platform.youtube{background:#dc2626}.platform.kick{color:#111;background:#53fc18}.platform.discord{background:#5865f2}.badges-right{order:5}.timestamp{font-size:10px}.message{min-width:0}.animation-fade .chat-line{animation:chat-fade both}.animation-slide .chat-line{animation:chat-slide both}.smooth .chat-lines{scroll-behavior:smooth}@keyframes chat-fade{from{opacity:0}to{opacity:1}}@keyframes chat-slide{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}
    .chat-widget footer{margin-top:8px;color:#94a3b8;font-size:10px}
    </style>
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///work/src/features/creator-tools/overlays/tests/creatorChatConfig.test.mjs:63:10)
    async Test.run (node:internal/test_runner/test:1054:7)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# (node:262) ExperimentalWarning: stripTypeScriptTypes is an experimental feature and might change at any time
# (Use `node --trace-warnings ...` to show where the warning was created)
# Subtest: all five dedicated registry widgets share presentation and have one canonical topic
ok 30 - all five dedicated registry widgets share presentation and have one canonical topic
  ---
  duration_ms: 16.020083
  type: 'test'
  ...
# Subtest: snapshot, backend validation and event fanout isolate every dedicated alert and preserve legacy alerts
ok 31 - snapshot, backend validation and event fanout isolate every dedicated alert and preserve legacy alerts
  ---
  duration_ms: 3.498761
  type: 'test'
  ...
# Subtest: Twitch Alerts and Overlay Builder edit the same Brand configuration in both directions
ok 32 - Twitch Alerts and Overlay Builder edit the same Brand configuration in both directions
  ---
  duration_ms: 61.6475
  type: 'test'
  ...
# Subtest: dedicated previews map canonical config and share local presentation without live event calls
ok 33 - dedicated previews map canonical config and share local presentation without live event calls
  ---
  duration_ms: 187.900818
  type: 'test'
  ...
# Subtest: alert config failures retain drafts and in-flight saves do not discard newer edits
ok 34 - alert config failures retain drafts and in-flight saves do not discard newer edits
  ---
  duration_ms: 2.954979
  type: 'test'
  ...
# Subtest: events are deterministic, provider-scoped, and routed by topic
ok 35 - events are deterministic, provider-scoped, and routed by topic
  ---
  duration_ms: 2.003667
  type: 'test'
  ...
# Subtest: approved assets retain their exact bytes and hosting MIME rules cover both formats
ok 36 - approved assets retain their exact bytes and hosting MIME rules cover both formats
  ---
  duration_ms: 22.894347
  type: 'test'
  ...
# Subtest: each supplied kind inherits its own media, audio and copy without persisting resolved values
ok 37 - each supplied kind inherits its own media, audio and copy without persisting resolved values
  ---
  duration_ms: 2.306804
  type: 'test'
  ...
# Subtest: custom fields win independently and resetting removes overrides while preserving other settings
ok 38 - custom fields win independently and resetting removes overrides while preserving other settings
  ---
  duration_ms: 0.547921
  type: 'test'
  ...
# Subtest: shared presentation survives asset failures and plays audio only on explicit triggers
ok 39 - shared presentation survives asset failures and plays audio only on explicit triggers
  ---
  duration_ms: 68.505212
  type: 'test'
  ...
# Subtest: runtime duration fallback preserves legacy fields and Redemption settings
ok 40 - runtime duration fallback preserves legacy fields and Redemption settings
  ---
  duration_ms: 1.911362
  type: 'test'
  ...
# Subtest: both editors expose reset and still render through the shared presentation
ok 41 - both editors expose reset and still render through the shared presentation
  ---
  duration_ms: 134.067355
  type: 'test'
  ...
# Subtest: builder state is deterministic, scene-specific and safely restorable
ok 42 - builder state is deterministic, scene-specific and safely restorable
  ---
  duration_ms: 3.320193
  type: 'test'
  ...
# Subtest: geometry clamps, resizes and locked state can be enforced by caller
ok 43 - geometry clamps, resizes and locked state can be enforced by caller
  ---
  duration_ms: 1.257759
  type: 'test'
  ...
# Subtest: themes switch through configuration
ok 44 - themes switch through configuration
  ---
  duration_ms: 0.187682
  type: 'test'
  ...
# Subtest: suggestions are deterministic, actionable and reversible
ok 45 - suggestions are deterministic, actionable and reversible
  ---
  duration_ms: 2.440366
  type: 'test'
  ...
# Subtest: activity routing reports enabled and missing widgets
ok 46 - activity routing reports enabled and missing widgets
  ---
  duration_ms: 0.291175
  type: 'test'
  ...
# Subtest: OBS concepts remain unmistakably demonstrations
ok 47 - OBS concepts remain unmistakably demonstrations
  ---
  duration_ms: 0.340789
  type: 'test'
  ...
# Subtest: game previews are local draft assets and preserve editor geometry
ok 48 - game previews are local draft assets and preserve editor geometry
  ---
  duration_ms: 0.658654
  type: 'test'
  ...
# Subtest: canvas renders eight explicit resize handles only for editable selections
ok 49 - canvas renders eight explicit resize handles only for editable selections
  ---
  duration_ms: 2.151379
  type: 'test'
  ...
# Subtest: canvas owns rendered widget pointer interaction and supports empty-space deselection
ok 50 - canvas owns rendered widget pointer interaction and supports empty-space deselection
  ---
  duration_ms: 0.216436
  type: 'test'
  ...
# Subtest: widget selection chrome is positioned by its individual frame, not normal canvas flow
ok 51 - widget selection chrome is positioned by its individual frame, not normal canvas flow
  ---
  duration_ms: 0.325068
  type: 'test'
  ...
# Subtest: pointer lifecycle captures, commits once, and cleans up cancellation and lost capture
ok 52 - pointer lifecycle captures, commits once, and cleans up cancellation and lost capture
  ---
  duration_ms: 0.156846
  type: 'test'
  ...
# Subtest: editor and publication management share explicit Brand bindings without first-Brand fallback
ok 53 - editor and publication management share explicit Brand bindings without first-Brand fallback
  ---
  duration_ms: 1.063917
  type: 'test'
  ...
# Subtest: Brand selection prioritises the route, then shared context, then a sole accessible Brand
ok 54 - Brand selection prioritises the route, then shared context, then a sole accessible Brand
  ---
  duration_ms: 0.37803
  type: 'test'
  ...
# Subtest: legacy one-Brand access can use the sole workspace without guessing among multiple workspaces
ok 55 - legacy one-Brand access can use the sole workspace without guessing among multiple workspaces
  ---
  duration_ms: 0.155737
  type: 'test'
  ...
# Subtest: Save Draft remains independent and successful saves clear dirty state
ok 56 - Save Draft remains independent and successful saves clear dirty state
  ---
  duration_ms: 0.148005
  type: 'test'
  ...
# Subtest: toolbar exposes the contextual Draft to Live actions without hiding Save Draft
ok 57 - toolbar exposes the contextual Draft to Live actions without hiding Save Draft
  ---
  duration_ms: 4.56838
  type: 'test'
  ...
# Subtest: Save and Update Live saves first, never publishes after a failed save, and preserves stale state on publication failure
ok 58 - Save and Update Live saves first, never publishes after a failed save, and preserves stale state on publication failure
  ---
  duration_ms: 0.336166
  type: 'test'
  ...
# Subtest: publication metadata is sent on create and update without credential rotation
ok 59 - publication metadata is sent on create and update without credential rotation
  ---
  duration_ms: 0.201774
  type: 'test'
  ...
# Subtest: legacy live state is unknown and scene changes make Live stale
not ok 60 - legacy live state is unknown and scene changes make Live stale
  ---
  duration_ms: 0.961146
  type: 'test'
  location: '/work/src/features/creator-tools/overlays/tests/overlayDraftLive.test.mjs:74:1'
  failureType: 'testCodeFailure'
  error: |-
    The input did not match the regular expression /Status unknown · update recommended/. Input:
    
    '<template>\r\n' +
      '  <section class="builder-panel outputs-panel">\r\n' +
      '    <header class="builder-panel__heading"><strong>◎ &nbsp; Draft &amp; Live Overlay</strong><small>Browser Source</small></header>\r\n' +
      '    <div class="publication-status" aria-live="polite">\r\n' +
      "      <span><b>Draft</b>{{ draftDirty ? 'Unsaved changes' : `Saved · Revision ${draftRevision}` }}</span>\r\n" +
      "      <span><b>Live Overlay</b>{{ !publicationId ? busy ? 'Checking publication…' : error ? 'Status unavailable' : 'Not published' : liveStatusUnknown ? 'Published · update recommended' : liveOutOfDate ? 'Published · Update available' : 'Published · Up to date' }}</span>\r\n" +
      '      <span><b>Last published</b>{{ lastPublishedLabel }}</span>\r\n' +
      '    </div>\r\n' +
      '    <article>\r\n' +
      '      <div>\r\n' +
      '        <b>Selected Scene</b>\r\n' +
      '        <span>{{ selectedSceneName }}<br>Transparent background · {{ resolution.width }} × {{ resolution.height }}</span>\r\n' +
      '        <small v-if="publicationId">Active: {{ activeSceneName || activeSceneId }} · Revision {{ revision }}</small>\r\n' +
      '        <small v-else-if="!busy && !error">No active Browser Source for this Brand</small>\r\n' +
      '      </div>\r\n' +
      '      <div class="source-actions">\r\n' +
      `        <button v-if="!publicationId && !error" :disabled="busy" @click="$emit('create')">Create Browser Source</button>\r\n` +
      `        <button v-if="error" :disabled="busy" @click="$emit('refresh')">Refresh status</button>\r\n` +
      '        <template v-if="publicationId">\r\n' +
      `          <button :disabled="busy" @click="$emit('update')">Save Changes</button>\r\n` +
      `          <button v-if="sourceUrl" @click="$emit('copy')">Copy URL</button>\r\n` +
      `          <button v-if="sourceUrl" @click="$emit('open')">Open</button>\r\n` +
      `          <button :disabled="busy" @click="$emit('rotate')">Rotate / Reissue URL</button>\r\n` +
      `          <button class="danger" :disabled="busy" @click="$emit('revoke')">Revoke</button>\r\n` +
      '        </template>\r\n' +
      '      </div>\r\n' +
      '    </article>\r\n' +
      '    <div v-if="publicationId && sourceUrl">\r\n' +
      '      <b>Browser Source URL</b>\r\n' +
      "      <p>{{ sourceUrlVisible ? sourceUrl : '••••••••••••••••••••••••••••••••' }}</p>\r\n" +
      `      <button :aria-pressed="sourceUrlVisible" @click="sourceUrlVisible = !sourceUrlVisible">{{ sourceUrlVisible ? 'Hide URL' : 'Show URL' }}</button>\r\n` +
      '    </div>\r\n' +
      '    <form v-if="publicationId && !sourceUrl" @submit.prevent="restoreUrl">\r\n' +
      '      <p>This Browser Source already exists. Import its current URL from OBS once to make it available here on future visits. This preserves your source and URL.</p>\r\n' +
      '      <label>Existing OBS Browser Source URL<input v-model.trim="existingUrl" type="password" autocomplete="off" required aria-label="Existing OBS Browser Source URL"></label>\r\n' +
      '      <button :disabled="busy || !existingUrl" type="submit">Restore existing URL</button>\r\n' +
      '    </form>\r\n' +
      '    <p v-if="error" role="alert">{{ error }}</p>\r\n' +
      "    <p>One stable URL renders the Brand's active scene. <strong>Scene resolution: {{ resolution.width }} × {{ resolution.height }}</strong> — use these exact Width and Height values in OBS.</p>\r\n" +
      '    <p>Save Draft preserves editable work. Updating Live publishes the selected saved scene to the stable OBS URL.</p>\r\n' +
      '    <ul class="browser-source-settings">\r\n' +
      '      <li>FPS: 30</li>\r\n' +
      '      <li>Custom CSS: none</li>\r\n' +
      '      <li>Shutdown source when not visible: recommended</li>\r\n' +
      '      <li>Refresh browser when scene becomes active: recommended, so the latest published revision is fetched</li>\r\n' +
      '    </ul>\r\n' +
      '  </section>\r\n' +
      '</template>\r\n' +
      '\r\n' +
      '<script setup>\r\n' +
      "import { computed, ref, watch } from 'vue'\r\n" +
      'const props =\r\n' +
      'defineProps({\r\n' +
      '  resolution: Object, sourceUrl: String, publicationId: String, revision: Number, busy: Boolean, error: String,\r\n' +
      '  selectedSceneId: String, selectedSceneName: String, activeSceneId: String, activeSceneName: String,\r\n' +
      '  draftDirty: Boolean, draftRevision: Number, liveOutOfDate: Boolean, liveStatusUnknown: Boolean, lastPublishedAt: String,\r\n' +
      '})\r\n' +
      "const emit = defineEmits(['create', 'update', 'replace', 'copy', 'open', 'rotate', 'revoke', 'preview', 'import', 'refresh'])\r\n" +
      "const existingUrl = ref('')\r\n" +
      "function restoreUrl() { emit('import', existingUrl.value); existingUrl.value = '' }\r\n" +
      'const sourceUrlVisible = ref(false)\r\n' +
      'watch(() => props.sourceUrl, () => { sourceUrlVisible.value = false })\r\n' +
      "const lastPublishedLabel = computed(() => props.lastPublishedAt ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(props.lastPublishedAt)) : 'Never')\r\n" +
      '</script>\r\n' +
      '\r\n' +
      '<style scoped>\r\n' +
      '.publication-status{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;padding:9px 10px;border-bottom:1px solid rgba(148,163,184,.1)}\r\n' +
      '.publication-status span{display:grid;gap:3px;color:#94a3b8;font-size:9px}.publication-status b{color:#ddd6fe;font-size:8px;text-transform:uppercase;letter-spacing:.06em}\r\n' +
      '</style>\r\n'
    
  code: 'ERR_ASSERTION'
  name: 'AssertionError'
  expected:
  actual: |-
    <template>
      <section class="builder-panel outputs-panel">
        <header class="builder-panel__heading"><strong>◎ &nbsp; Draft &amp; Live Overlay</strong><small>Browser Source</small></header>
        <div class="publication-status" aria-live="polite">
          <span><b>Draft</b>{{ draftDirty ? 'Unsaved changes' : `Saved · Revision ${draftRevision}` }}</span>
          <span><b>Live Overlay</b>{{ !publicationId ? busy ? 'Checking publication…' : error ? 'Status unavailable' : 'Not published' : liveStatusUnknown ? 'Published · update recommended' : liveOutOfDate ? 'Published · Update available' : 'Published · Up to date' }}</span>
          <span><b>Last published</b>{{ lastPublishedLabel }}</span>
        </div>
        <article>
          <div>
            <b>Selected Scene</b>
            <span>{{ selectedSceneName }}<br>Transparent background · {{ resolution.width }} × {{ resolution.height }}</span>
            <small v-if="publicationId">Active: {{ activeSceneName || activeSceneId }} · Revision {{ revision }}</small>
            <small v-else-if="!busy && !error">No active Browser Source for this Brand</small>
          </div>
          <div class="source-actions">
            <button v-if="!publicationId && !error" :disabled="busy" @click="$emit('create')">Create Browser Source</button>
            <button v-if="error" :disabled="busy" @click="$emit('refresh')">Refresh status</button>
            <template v-if="publicationId">
              <button :disabled="busy" @click="$emit('update')">Save Changes</button>
              <button v-if="sourceUrl" @click="$emit('copy')">Copy URL</button>
              <button v-if="sourceUrl" @click="$emit('open')">Open</button>
              <button :disabled="busy" @click="$emit('rotate')">Rotate / Reissue URL</button>
              <button class="danger" :disabled="busy" @click="$emit('revoke')">Revoke</button>
            </template>
          </div>
        </article>
        <div v-if="publicationId && sourceUrl">
          <b>Browser Source URL</b>
          <p>{{ sourceUrlVisible ? sourceUrl : '••••••••••••••••••••••••••••••••' }}</p>
          <button :aria-pressed="sourceUrlVisible" @click="sourceUrlVisible = !sourceUrlVisible">{{ sourceUrlVisible ? 'Hide URL' : 'Show URL' }}</button>
        </div>
        <form v-if="publicationId && !sourceUrl" @submit.prevent="restoreUrl">
          <p>This Browser Source already exists. Import its current URL from OBS once to make it available here on future visits. This preserves your source and URL.</p>
          <label>Existing OBS Browser Source URL<input v-model.trim="existingUrl" type="password" autocomplete="off" required aria-label="Existing OBS Browser Source URL"></label>
          <button :disabled="busy || !existingUrl" type="submit">Restore existing URL</button>
        </form>
        <p v-if="error" role="alert">{{ error }}</p>
        <p>One stable URL renders the Brand's active scene. <strong>Scene resolution: {{ resolution.width }} × {{ resolution.height }}</strong> — use these exact Width and Height values in OBS.</p>
        <p>Save Draft preserves editable work. Updating Live publishes the selected saved scene to the stable OBS URL.</p>
        <ul class="browser-source-settings">
          <li>FPS: 30</li>
          <li>Custom CSS: none</li>
          <li>Shutdown source when not visible: recommended</li>
          <li>Refresh browser when scene becomes active: recommended, so the latest published revision is fetched</li>
        </ul>
      </section>
    </template>
    
    <script setup>
    import { computed, ref, watch } from 'vue'
    const props =
    defineProps({
      resolution: Object, sourceUrl: String, publicationId: String, revision: Number, busy: Boolean, error: String,
      selectedSceneId: String, selectedSceneName: String, activeSceneId: String, activeSceneName: String,
      draftDirty: Boolean, draftRevision: Number, liveOutOfDate: Boolean, liveStatusUnknown: Boolean, lastPublishedAt: String,
    })
    const emit = defineEmits(['create', 'update', 'replace', 'copy', 'open', 'rotate', 'revoke', 'preview', 'import', 'refresh'])
    const existingUrl = ref('')
    function restoreUrl() { emit('import', existingUrl.value); existingUrl.value = '' }
    const sourceUrlVisible = ref(false)
    watch(() => props.sourceUrl, () => { sourceUrlVisible.value = false })
    const lastPublishedLabel = computed(() => props.lastPublishedAt ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(props.lastPublishedAt)) : 'Never')
    </script>
    
    <style scoped>
    .publication-status{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;padding:9px 10px;border-bottom:1px solid rgba(148,163,184,.1)}
    .publication-status span{display:grid;gap:3px;color:#94a3b8;font-size:9px}.publication-status b{color:#ddd6fe;font-size:8px;text-transform:uppercase;letter-spacing:.06em}
    </style>
    
  operator: 'match'
  stack: |-
    TestContext.<anonymous> (file:///work/src/features/creator-tools/overlays/tests/overlayDraftLive.test.mjs:76:10)
    Test.runInAsyncScope (node:async_hooks:214:14)
    Test.run (node:internal/test_runner/test:1047:25)
    Test.processPendingSubtests (node:internal/test_runner/test:744:18)
    Test.postRun (node:internal/test_runner/test:1173:19)
    Test.run (node:internal/test_runner/test:1101:12)
    async Test.processPendingSubtests (node:internal/test_runner/test:744:7)
  ...
# Subtest: editor loads and saves a Brand-scoped server project without autosaving defaults
ok 61 - editor loads and saves a Brand-scoped server project without autosaving defaults
  ---
  duration_ms: 1.553349
  type: 'test'
  ...
# Subtest: failed and stale saves remain visibly dirty while publish stays explicit and stable
ok 62 - failed and stale saves remain visibly dirty while publish stays explicit and stable
  ---
  duration_ms: 0.201745
  type: 'test'
  ...
# Subtest: GET stores the response revision and consecutive saves use each server-returned revision
ok 63 - GET stores the response revision and consecutive saves use each server-returned revision
  ---
  duration_ms: 27.24942
  type: 'test'
  ...
# Subtest: the existing saving guard prevents overlapping requests with the same revision
ok 64 - the existing saving guard prevents overlapping requests with the same revision
  ---
  duration_ms: 7.698466
  type: 'test'
  ...
# Subtest: successful PUT advances revision before returned project processing can fail
ok 65 - successful PUT advances revision before returned project processing can fail
  ---
  duration_ms: 6.789821
  type: 'test'
  ...
# Subtest: stale saves preserve local edits, dirty state and revision and surface the error without retrying
ok 66 - stale saves preserve local edits, dirty state and revision and surface the error without retrying
  ---
  duration_ms: 7.257525
  type: 'test'
  ...
# Subtest: overlay entry prefers the remembered project
ok 67 - overlay entry prefers the remembered project
  ---
  duration_ms: 1.037414
  type: 'test'
  ...
# Subtest: overlay entry defaults to Main Gameplay when no recent project exists
ok 68 - overlay entry defaults to Main Gameplay when no recent project exists
  ---
  duration_ms: 0.273768
  type: 'test'
  ...
# Subtest: overlay entry ignores a removed remembered project
ok 69 - overlay entry ignores a removed remembered project
  ---
  duration_ms: 0.143771
  type: 'test'
  ...
# Subtest: routes normalized chat.message
ok 70 - routes normalized chat.message
  ---
  duration_ms: 1.090044
  type: 'test'
  ...
# Subtest: routes normalized stream.follow
ok 71 - routes normalized stream.follow
  ---
  duration_ms: 0.276347
  type: 'test'
  ...
# Subtest: routes normalized stream.subscription
ok 72 - routes normalized stream.subscription
  ---
  duration_ms: 1.476814
  type: 'test'
  ...
# Subtest: routes normalized stream.raid
ok 73 - routes normalized stream.raid
  ---
  duration_ms: 0.155764
  type: 'test'
  ...
# Subtest: routes normalized stream.cheer
ok 74 - routes normalized stream.cheer
  ---
  duration_ms: 0.252946
  type: 'test'
  ...
# Subtest: routes normalized reward.redeemed
ok 75 - routes normalized reward.redeemed
  ---
  duration_ms: 0.106672
  type: 'test'
  ...
# Subtest: routes normalized tts.requested
ok 76 - routes normalized tts.requested
  ---
  duration_ms: 0.179419
  type: 'test'
  ...
# Subtest: rejects malformed, unversioned, and unsupported events
ok 77 - rejects malformed, unversioned, and unsupported events
  ---
  duration_ms: 0.828676
  type: 'test'
  ...
# Subtest: canvas uses stable native coordinates at visual scale
ok 78 - canvas uses stable native coordinates at visual scale
  ---
  duration_ms: 8.043243
  type: 'test'
  ...
# Subtest: rendered canvas bounds produce independent drag scales
ok 79 - rendered canvas bounds produce independent drag scales
  ---
  duration_ms: 4.790364
  type: 'test'
  ...
# Subtest: physical pointer-edge detection returns all eight resize directions and leaves the centre clear
ok 80 - physical pointer-edge detection returns all eight resize directions and leaves the centre clear
  ---
  duration_ms: 0.305015
  type: 'test'
  ...
# Subtest: edge detection caps its threshold to preserve the centre of a small widget
ok 81 - edge detection caps its threshold to preserve the centre of a small widget
  ---
  duration_ms: 0.126306
  type: 'test'
  ...
# Subtest: chat showcase target moves to the opposite safe side without changing size
ok 82 - chat showcase target moves to the opposite safe side without changing size
  ---
  duration_ms: 0.258775
  type: 'test'
  ...
# Subtest: movement clamps and snaps to canvas edges
ok 83 - movement clamps and snaps to canvas edges
  ---
  duration_ms: 0.358815
  type: 'test'
  ...
# Subtest: movement remains inside every canvas boundary without snapping
ok 84 - movement remains inside every canvas boundary without snapping
  ---
  duration_ms: 0.327935
  type: 'test'
  ...
# Subtest: movement snaps centres and reports the actual canvas guide coordinate
ok 85 - movement snaps centres and reports the actual canvas guide coordinate
  ---
  duration_ms: 0.178678
  type: 'test'
  ...
# Subtest: movement snaps edges and centres to other widget alignment lines
ok 86 - movement snaps edges and centres to other widget alignment lines
  ---
  duration_ms: 0.45738
  type: 'test'
  ...
# Subtest: resize respects minimum and canvas bounds
ok 87 - resize respects minimum and canvas bounds
  ---
  duration_ms: 0.458703
  type: 'test'
  ...
# Subtest: all eight resize handles change the expected axes
ok 88 - all eight resize handles change the expected axes
  ---
  duration_ms: 0.290554
  type: 'test'
  ...
# Subtest: north and south resize preserve their opposite edge in both directions
ok 89 - north and south resize preserve their opposite edge in both directions
  ---
  duration_ms: 0.146393
  type: 'test'
  ...
# Subtest: widget minimum size is respected from anchored sides
ok 90 - widget minimum size is respected from anchored sides
  ---
  duration_ms: 0.086437
  type: 'test'
  ...
# Subtest: widget minimum width and height are enforced independently
ok 91 - widget minimum width and height are enforced independently
  ---
  duration_ms: 0.082405
  type: 'test'
  ...
# Subtest: resize stays within top, left, right and bottom canvas boundaries
ok 92 - resize stays within top, left, right and bottom canvas boundaries
  ---
  duration_ms: 0.081584
  type: 'test'
  ...
# Subtest: keyboard nudging uses 1px or 10px, clamps to bounds, and rejects locked widgets
ok 93 - keyboard nudging uses 1px or 10px, clamps to bounds, and rejects locked widgets
  ---
  duration_ms: 0.268474
  type: 'test'
  ...
# Subtest: resized edges snap to other widgets and report the alignment coordinate
ok 94 - resized edges snap to other widgets and report the alignment coordinate
  ---
  duration_ms: 0.218653
  type: 'test'
  ...
# Subtest: resolution scaling preserves proportional geometry
ok 95 - resolution scaling preserves proportional geometry
  ---
  duration_ms: 0.150534
  type: 'test'
  ...
# Subtest: history supports undo, redo, branching and a cap
ok 96 - history supports undo, redo, branching and a cap
  ---
  duration_ms: 1.155636
  type: 'test'
  ...
# Subtest: history snapshots Vue reactive editor state without cloning the Proxy
ok 97 - history snapshots Vue reactive editor state without cloning the Proxy
  ---
  duration_ms: 1.588064
  type: 'test'
  ...
# Subtest: overlay snapshots exclude runtime callbacks and retain editable data
ok 98 - overlay snapshots exclude runtime callbacks and retain editable data
  ---
  duration_ms: 1.864938
  type: 'test'
  ...
# Subtest: a transient canvas interaction creates exactly one history entry when committed
ok 99 - a transient canvas interaction creates exactly one history entry when committed
  ---
  duration_ms: 0.520673
  type: 'test'
  ...
# Subtest: versioned state round trips without session image URLs
ok 100 - versioned state round trips without session image URLs
  ---
  duration_ms: 1.120718
  type: 'test'
  ...
# Subtest: malformed and old storage safely recover
ok 101 - malformed and old storage safely recover
  ---
  duration_ms: 1.767871
  type: 'test'
  ...
# Subtest: missing storage uses starter state without presenting a migration
ok 102 - missing storage uses starter state without presenting a migration
  ---
  duration_ms: 1.264223
  type: 'test'
  ...
# Subtest: publication materializes computed alert display modes without mutating editor state
ok 103 - publication materializes computed alert display modes without mutating editor state
  ---
  duration_ms: 1.776378
  type: 'test'
  ...
# Subtest: explicit canonical widget topics are retained
ok 104 - explicit canonical widget topics are retained
  ---
  duration_ms: 1.530864
  type: 'test'
  ...
# Subtest: renderer derives the same triggered default for legacy alert snapshots
ok 105 - renderer derives the same triggered default for legacy alert snapshots
  ---
  duration_ms: 0.228251
  type: 'test'
  ...
# Subtest: event-driven widgets cannot publish unsafe always-visible demo state
ok 106 - event-driven widgets cannot publish unsafe always-visible demo state
  ---
  duration_ms: 0.458442
  type: 'test'
  ...
# Subtest: publication excludes disabled, hidden, and physically deleted widgets
ok 107 - publication excludes disabled, hidden, and physically deleted widgets
  ---
  duration_ms: 0.436143
  type: 'test'
  ...
# Subtest: one browser renderer renders all enabled scene widgets with exact frames and z order
ok 108 - one browser renderer renders all enabled scene widgets with exact frames and z order
  ---
  duration_ms: 1.267971
  type: 'test'
  ...
# Subtest: browser renderer is transparent and contains no editor chrome
ok 109 - browser renderer is transparent and contains no editor chrome
  ---
  duration_ms: 0.383131
  type: 'test'
  ...
# Subtest: browser renderer scopes triggered visibility to canonical widget topics
ok 110 - browser renderer scopes triggered visibility to canonical widget topics
  ---
  duration_ms: 0.157098
  type: 'test'
  ...
# Subtest: Overlay Builder exposes one scene publication lifecycle and no per-widget URLs
ok 111 - Overlay Builder exposes one scene publication lifecycle and no per-widget URLs
  ---
  duration_ms: 1.200791
  type: 'test'
  ...
# Subtest: Test Chat is local while other Creator Tools tests retain the shared server event boundary
ok 112 - Test Chat is local while other Creator Tools tests retain the shared server event boundary
  ---
  duration_ms: 0.422446
  type: 'test'
  ...
# Subtest: Browser Source retrieves server configuration and opens one reconnecting scene connection
ok 113 - Browser Source retrieves server configuration and opens one reconnecting scene connection
  ---
  duration_ms: 0.334568
  type: 'test'
  ...
# Subtest: Browser Source transparency is isolated to its route lifecycle
ok 114 - Browser Source transparency is isolated to its route lifecycle
  ---
  duration_ms: 0.206958
  type: 'test'
  ...
# Subtest: 1920x1080 scene is unchanged in a matching OBS viewport
ok 115 - 1920x1080 scene is unchanged in a matching OBS viewport
  ---
  duration_ms: 1.974366
  type: 'test'
  ...
# Subtest: 1920x1080 scene scales uniformly to 1280x720 without coordinate drift
ok 116 - 1920x1080 scene scales uniformly to 1280x720 without coordinate drift
  ---
  duration_ms: 0.233234
  type: 'test'
  ...
# Subtest: mismatched viewport letterboxes and centers rather than stretching
ok 117 - mismatched viewport letterboxes and centers rather than stretching
  ---
  duration_ms: 0.161291
  type: 'test'
  ...
# Subtest: overlay operations rename, duplicate and protect the final layout
ok 118 - overlay operations rename, duplicate and protect the final layout
  ---
  duration_ms: 1.086566
  type: 'test'
  ...
# Subtest: runtime serialization excludes editor backgrounds and credentials
ok 119 - runtime serialization excludes editor backgrounds and credentials
  ---
  duration_ms: 1.449242
  type: 'test'
  ...
# Subtest: stream.follow reaches its matching triggered alert
ok 120 - stream.follow reaches its matching triggered alert
  ---
  duration_ms: 2.60711
  type: 'test'
  ...
# Subtest: stream.subscription reaches its matching triggered alert
ok 121 - stream.subscription reaches its matching triggered alert
  ---
  duration_ms: 0.26041
  type: 'test'
  ...
# Subtest: stream.raid reaches its matching triggered alert
ok 122 - stream.raid reaches its matching triggered alert
  ---
  duration_ms: 0.117284
  type: 'test'
  ...
# Subtest: unrelated events do not trigger a widget
ok 123 - unrelated events do not trigger a widget
  ---
  duration_ms: 0.127426
  type: 'test'
  ...
# Subtest: canonical runtime settings suppress disabled alerts and override snapshot duration
ok 124 - canonical runtime settings suppress disabled alerts and override snapshot duration
  ---
  duration_ms: 0.463086
  type: 'test'
  ...
# Subtest: a second matching event resets the timeout and can trigger again after hiding
ok 125 - a second matching event resets the timeout and can trigger again after hiding
  ---
  duration_ms: 0.335004
  type: 'test'
  ...
# Subtest: chat messages continue through the shared event bus to chat widgets
ok 126 - chat messages continue through the shared event bus to chat widgets
  ---
  duration_ms: 0.317734
  type: 'test'
  ...
# Subtest: Send Test remains actionable so missing Brand or configuration context produces a visible reason
ok 127 - Send Test remains actionable so missing Brand or configuration context produces a visible reason
  ---
  duration_ms: 1.278499
  type: 'test'
  ...
# Subtest: Send Test visibly blocks disabled alert configuration and explains zero delivery
ok 128 - Send Test visibly blocks disabled alert configuration and explains zero delivery
  ---
  duration_ms: 0.300629
  type: 'test'
  ...
# Subtest: Builder Test Chat injects locally and renders without publication or Twitch access
ok 129 - Builder Test Chat injects locally and renders without publication or Twitch access
  ---
  duration_ms: 92.665906
  type: 'test'
  ...
# Subtest: the same chat renderer handles canonical events, bursts, duplicates, filtering and disable/removal
ok 130 - the same chat renderer handles canonical events, bursts, duplicates, filtering and disable/removal
  ---
  duration_ms: 38.154188
  type: 'test'
  ...
# Subtest: editor live subscription is Brand-bound, discards stale callbacks, and exposes safe offline/error states
ok 131 - editor live subscription is Brand-bound, discards stale callbacks, and exposes safe offline/error states
  ---
  duration_ms: 9.036639
  type: 'test'
  ...
# Subtest: canonical alert configuration controls enablement, duration, templates and event payload interpolation
ok 132 - canonical alert configuration controls enablement, duration, templates and event payload interpolation
  ---
  duration_ms: 0.942063
  type: 'test'
  ...
# Subtest: TTS and chat use server-backed safe configuration without Browser Source localStorage
ok 133 - TTS and chat use server-backed safe configuration without Browser Source localStorage
  ---
  duration_ms: 0.273148
  type: 'test'
  ...
# Subtest: Overlay Builder retains layout and visual controls but removes duplicate Twitch behaviour ownership
ok 134 - Overlay Builder retains layout and visual controls but removes duplicate Twitch behaviour ownership
  ---
  duration_ms: 0.237729
  type: 'test'
  ...
# Subtest: Unified Chat movement updates X/Y and remains one undoable transaction
ok 135 - Unified Chat movement updates X/Y and remains one undoable transaction
  ---
  duration_ms: 3.735282
  type: 'test'
  ...
# Subtest: Demo Chat Move chooses the opposite safe side at Fit-scale-independent geometry
ok 136 - Demo Chat Move chooses the opposite safe side at Fit-scale-independent geometry
  ---
  duration_ms: 0.504187
  type: 'test'
  ...
# Subtest: canvas separates body movement from explicit resize handles with capability and locked fallbacks
ok 137 - canvas separates body movement from explicit resize handles with capability and locked fallbacks
  ---
  duration_ms: 0.702975
  type: 'test'
  ...
# Subtest: canvas interaction CSS has eight-handle controls and keeps a non-interactive frame-filling renderer
ok 138 - canvas interaction CSS has eight-handle controls and keeps a non-interactive frame-filling renderer
  ---
  duration_ms: 1.301137
  type: 'test'
  ...
# Subtest: explicit resize handles use pointer capture and standard cursors
ok 139 - explicit resize handles use pointer capture and standard cursors
  ---
  duration_ms: 0.391194
  type: 'test'
  ...
# Subtest: registry declares the approved stable widget types without persisted component names
ok 140 - registry declares the approved stable widget types without persisted component names
  ---
  duration_ms: 13.320518
  type: 'test'
  ...
# Subtest: registry normalizes interaction capabilities and minimum sizes
ok 141 - registry normalizes interaction capabilities and minimum sizes
  ---
  duration_ms: 0.950436
  type: 'test'
  ...
# Subtest: registry assigns safe display modes to event-driven widgets
ok 142 - registry assigns safe display modes to event-driven widgets
  ---
  duration_ms: 0.200422
  type: 'test'
  ...
1..142
# tests 142
# suites 0
# pass 139
# fail 3
# cancelled 0
# skipped 0
# todo 0
# duration_ms 2173.183784
